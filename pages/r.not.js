module.exports = () => {
    html
        $scope.title = $scope.document.title;
        -include("../partials/header.not.js")
        body["container-fluid"];
            $($scope.rendered, true);
        $body
    $html
}